import { coreColors, coreFonts } from './cores'

const bucketRowContainer = {
  width: '100%',
  height: '100px', // instead of 107 in wireframe
  display: 'flex'
}

export const BucketRowStyles = {
  bucketRowContainer
}
